"""
Name: Ahmed Affaan                                  
Title: pseudo_code.txt
Folder: DADSA_ASSIGNMENT                            
Date: 10/03/2022                                    
Country: Republic of Maldives                                                       
Description: DADSA year 02 Assignment - UWE Bristol BSc Computer Science
Student Number(UWE): 19045165
Student ID:(VC): 1802035                                                                
"""

# PROGRAM START.

#### ----VARIABLES---- ####
dhoaniCapacity = 30000
supplierIslandAlphaCapacity = 30000
supplierIslandBetaCapacity = 30000
islandA_Capacity = 30000
islandB_Capacity = 30000
islandC_Capacity = 30000
islandD_Capacity = 30000
#### ----VARIABLES---- ####

#### ----LISTS---- ####
# Lists for Dhoani's Item Name and Item Amount.
SET dhoaniItemName TO []
SET dhoaniItemAmount TO []
# Lists for Supplier Island 01 Item Name and Item Amount.
SET supplierIslandAlphaItemName TO []
SET supplierIslandAlphaItemAmount TO []
# Lists for Supplier Island 02 Item Name and Item Amount.
SET supplierIslandBetaItemName TO []
SET supplierIslandBetaItemAmount TO []
# Lists for Island A Item Name and Item Amount.
SET islandA_ItemName TO []
SET islandA_ItemAmount TO []
# Lists for Island B Item Name and Item Amount.
SET islandB_ItemName TO []
SET islandB_ItemAmount TO []
# Lists for Island C Item Name and Item Amount.
SET islandC_ItemName TO []
SET islandC_ItemAmount TO []
# Lists for Island D Item Name and Item Amount.
SET islandD_ItemName TO []
SET islandD_ItemAmount TO []
#### ----LISTS---- ####

#### ----CLASSES---- ####
# This class will display Dhoani's drive and stop messages IN the functions.
DEFINE CLASS DhoaniTravel:
    # Dhoani's function messages.
    SET drive TO "Dhoani is driving"
    SET stop TO "Dhoani has stopped"

    # Dhoani's drive function.
    DEFINE FUNCTION get_drive(self):
        # Prints message of the Dhoani's function.
        RETURN self.drive

    # Dhoani's stop function.
    DEFINE FUNCTION get_stop(self):
        # Prints message of the Dhoani's function.
        RETURN self.stop
#### ----CLASSES---- ####

# Assigning class to a variable.
SET dhoani1 TO DhoaniTravel()

#### ----FUNCTIONS---- ####
# This function displays the main console menu.
# Option 5 is continue. If 6 is not chosen it selects 5.
DEFINE FUNCTION consoleMenu():
    OUTPUT("")
    OUTPUT("---------------------------------")
    OUTPUT("|      BigCon Construction      |")
    OUTPUT("--------------Menu---------------")
    OUTPUT("1- Add Items                    |")
    OUTPUT("2- Remove Items                 |")
    OUTPUT("3- Search Items                 |")
    OUTPUT("4- Print Items                  |")
    OUTPUT("6- QUIT                         |")
    OUTPUT("---------------------------------")

    # Asks for initial user input.
    SET user_INPUT TO int(INPUT("Enter choice: "))
    consoleChoice(user_input)

# This function displays the Items Menu with their group code.
# Entering group code will not work. Enter the name displayed IN the menu.
DEFINE FUNCTION itemsMenu():
    OUTPUT("---------------------------------")
    OUTPUT("|      BigCon Construction      |")
    OUTPUT("--------------Items--------------")
    OUTPUT("1- Diesel                       |")
    OUTPUT("2- Frozen                       |")
    OUTPUT("3- Fridge                       |")
    OUTPUT("4- Food                         |")
    OUTPUT("5- Protected Material           |")
    OUTPUT("6- Unprotected Material         |")
    OUTPUT("---------------------------------")

# This function displays the Destinations menu.
DEFINE FUNCTION destinationMenu():
    OUTPUT("---------------------------------")
    OUTPUT("|      BigCon Construction      |")
    OUTPUT("----------Destinations-----------")
    OUTPUT("Dhoani                          |")
    OUTPUT("Supply Island 01                |")
    OUTPUT("Supply Island 02                |")
    OUTPUT("Island A                        |")
    OUTPUT("Island B                        |")
    OUTPUT("Island C                        |")
    OUTPUT("Island D                        |")
    OUTPUT("---------------------------------")

# This function checks the users specific choice and directs them to that function.
DEFINE FUNCTION consoleChoice(user_input):
    IF user_input EQUALS 1:
        addItems()
    ELSEIF user_input EQUALS 2:
        removeItems()
    ELSEIF user_input EQUALS 3:
        searchItems()
    ELSEIF user_input EQUALS 4:
        printItems()
    ELSEIF user_input EQUALS 6:
        exit()

# This function will add items.
DEFINE FUNCTION addItems():
    # Prints the Items Menu.
    OUTPUT("")    
    itemsMenu()
    # Asks for user input.
    SET itemName TO INPUT("Item Name: ").lower()
    SET itemAmount TO int(INPUT("Item Amount: "))

    # Checks if Item Name and Item Amount is valid and adds it.
    if((itemName != ["diesel", "frozen", "fridge", "food", "protected material", "unprotected material"]) and (itemAmount > 30000)):
        OUTPUT("INVALID INPUT")
    ELSE:
        # Adds Item Name and Item Amount in the list.
        OUTPUT("")
        OUTPUT("----ADDING ITEM!----")
        dhoaniItemName.append(itemName)
        dhoaniItemAmount.append(itemAmount)
        # This will calculate and OUTPUT the Dhoani's current capacity left.
        SET capacityCalculate TO dhoaniCapacity - itemAmount
        OUTPUT("Capacity left: " + str(capacityCalculate) + "KG")
        # Prints message of Dhoani travel function.
        OUTPUT(dhoani1.get_drive())

    # Asks user if they wish to continue or quit.
    OUTPUT("---------------------------------")
    SET user_input TO int(INPUT("Continue (5) or Quit (6): "))
    OUTPUT("")
    IF user_input EQUALS 5:
        consoleMenu()
    ELSE:
        exit()

# This function will remove items.
DEFINE FUNCTION removeItems():
    # Prints the Items Menu.
    OUTPUT("")
    itemsMenu()
    # Asks for user input
    SET itemName TO INPUT("Item Name: ").lower()
    SET itemAmount TO int(INPUT("Item Amount: "))

    # Checks if Item Name and Item Amount is valid and removes it.
    if((itemName != ["diesel", "frozen", "fridge", "food", "protected material", "unprotected material"]) and (itemAmount > 30000)):
        OUTPUT("INVALID INPUT")
    ELSE:
        IF ((itemName IN dhoaniItemName) and (itemAmount IN dhoaniItemAmount)):  
            # Removing Item Name and Item Amount.
            OUTPUT("----REMOVING ITEM!----")  
            dhoaniItemName.remove(itemName)
            dhoaniItemAmount.remove(itemAmount)

            # Asking user if they wish to continue or quit.
            OUTPUT("---------------------------------")
            SET user_input TO int(INPUT("Continue (5) or Exit (6): "))
            OUTPUT("")
            IF user_input EQUALS 5:
               consoleMenu()
            ELSE:
               exit()
        ELSE:
            # Error message.
            OUTPUT("INVALID INPUT")

# This function will search through the lists and prints items and their amounts.
DEFINE FUNCTION searchItems():
    OUTPUT("----SEARCHING ITEMS----")
    OUTPUT("---------------------------------")
    SET itemName TO INPUT("Item Name: ")
    SET itemAmount TO int(INPUT("Item Amount: "))

    # Checks if the entered elements exists in the lists and prints them.
    IF ((itemName IN dhoaniItemName) and (itemAmount IN dhoaniItemAmount)):
        OUTPUT("")
        OUTPUT("----ITEM EXISTS!----")
        OUTPUT("Item Name:", dhoaniItemName)
        OUTPUT("Item Amount:", dhoaniItemAmount)
    ELSE:
        # Error message.
        OUTPUT("INVALID INPUT")

    # Asking user if they wish to continue or quit.
    OUTPUT("---------------------------------")
    SET user_input TO int(INPUT("Continue (5) or Exit (6): "))
    OUTPUT("")
    IF user_input EQUALS 5:
        consoleMenu()
    ELSE:
        exit()

# This function will print all of the lists and their amounts.
DEFINE FUNCTION printItems():
    # Dhoani inventory.
    OUTPUT("")
    OUTPUT("Dhoani Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", dhoaniItemName)
    OUTPUT("Amount:", dhoaniItemAmount)
    OUTPUT("-----------------------------")

    # Supply Island 01 inventory.
    OUTPUT("")
    OUTPUT("Supply Island 01 Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", supplierIslandAlphaItemName)
    OUTPUT("Amount:", supplierIslandAlphaItemAmount)
    OUTPUT("-----------------------------")

    # Supply Island 02 inventory.
    OUTPUT("")
    OUTPUT("Supply Island 02 Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", supplierIslandBetaItemName)
    OUTPUT("Amount:", supplierIslandBetaItemAmount)
    OUTPUT("-----------------------------")

    # Island A inventory.
    OUTPUT("")
    OUTPUT("Supply Island A Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", islandA_ItemName)
    OUTPUT("Amount:", islandA_ItemAmount)
    OUTPUT("-----------------------------")

    # Island B inventory.
    OUTPUT("")
    OUTPUT("Supply Island B Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", islandB_ItemName)
    OUTPUT("Amount:", islandB_ItemAmount)
    OUTPUT("-----------------------------")

    # Island C inventory.
    OUTPUT("")
    OUTPUT("Supply Island C Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", islandC_ItemName)
    OUTPUT("Amount:", islandC_ItemAmount)
    OUTPUT("-----------------------------")

    # Island D Inventory.
    OUTPUT("")
    OUTPUT("Supply Island D Inventory")
    OUTPUT("-----------------------------")
    OUTPUT("Items: ", islandD_ItemName)
    OUTPUT("Amount:", islandD_ItemAmount)
    OUTPUT("-----------------------------")

    # Asking user if they wish to continue or quit.
    SET user_input TO int(INPUT("Continue (5) or Quit (6): "))
    IF user_input EQUALS 5:
        consoleMenu()
    ELSE:
        exit()
#### ----FUNCTIONS---- ####

# Calling the function globally.
# Displays main console menu.
consoleMenu()

# PROGRAM END.
